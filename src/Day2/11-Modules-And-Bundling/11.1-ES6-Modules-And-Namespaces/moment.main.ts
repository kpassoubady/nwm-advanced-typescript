//moment.main.ts
import * as moment from "moment";

/*
we create an ambient module declaration file moment.d.ts that declares the shape of the moment module 
from the moment.js library. 
This allows us to import and use the moment module in our TypeScript code with type information
*/

//const today = moment().format("YYYY-MM-DD");
//console.log(today); // Output: Current date in YYYY-MM-DD format
